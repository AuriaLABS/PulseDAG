[package]
name = "pulsedag-api"
version.workspace = true
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
pulsedag-core = { path = "../pulsedag-core" }

[dev-dependencies]
serde_json = "1"
