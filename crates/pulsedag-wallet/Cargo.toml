[package]
name = "pulsedag-wallet"
version = "2.2.0"
edition = "2021"

[dependencies]
pulsedag-core = { path = "../pulsedag-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
