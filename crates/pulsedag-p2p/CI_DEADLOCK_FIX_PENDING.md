# Temporary CI repair marker

This file forces the focused P2P regression workflow to run with the already-installed write-enabled definition. The workflow removes this marker in the same commit that applies the confirmed recursive-lock fix.
