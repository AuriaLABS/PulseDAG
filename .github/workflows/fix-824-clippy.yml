name: fix-824-clippy

on:
  push:
    branches:
      - fix/2.4.x-prune-boundary-824

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Clippy cleanup
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/fix_824_clippy.py
          cargo fmt --all
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: satisfy Clippy for #824 peer compatibility"
          git push origin HEAD:fix/2.4.x-prune-boundary-824
      - name: Full lint validation
        shell: bash
        run: |
          set -euo pipefail
          cargo clippy --all-targets --all-features -- -D warnings
