name: Sync #1071 with current main
on:
  push:
    branches: [codex/issue-1036-mempool-policy-v3-foundation]
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-1036-mempool-policy-v3-foundation
          fetch-depth: 0
      - name: Merge current main and publish clean head
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'kalekoi'
          git config user.email 'hermida.prada@gmail.com'
          git fetch origin main
          git merge --no-edit origin/main
          rm .github/workflows/sync-1071-main.yml
          git add .github/workflows/sync-1071-main.yml
          if ! git diff --cached --quiet; then
            git commit -m 'ci: remove #1071 sync helper'
          fi
          git diff --check
          git push origin HEAD:codex/issue-1036-mempool-policy-v3-foundation
