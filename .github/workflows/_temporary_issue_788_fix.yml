name: Issue 788 trusted applicator trigger

on:
  pull_request:
    branches:
      - release/2.4.0
    paths:
      - .github/workflows/_temporary_issue_788_fix.yml

permissions:
  contents: read

jobs:
  marker:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo "The trusted base workflow applies and validates the issue 788 patch."
