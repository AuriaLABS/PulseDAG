name: PR1062 rebase helper

on:
  push:
    branches:
      - codex/pr1062-bootstrap-768a04b

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: echo PR1062-helper-ok
