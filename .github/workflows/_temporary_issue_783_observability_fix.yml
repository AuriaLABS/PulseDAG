name: Issue 783 observability fix trigger

on:
  pull_request:
    branches:
      - release/2.4.0
    paths:
      - .github/workflows/_temporary_issue_783_observability_fix.yml

permissions:
  contents: read

jobs:
  marker:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo "A trusted base workflow applies and validates the observability route fix."
