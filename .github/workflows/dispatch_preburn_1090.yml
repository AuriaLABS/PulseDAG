name: Dispatch exact-head pre-burn for PR 1090

on:
  push:
    branches:
      - ops/preburn-1090-dispatch

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    if: "github.event.head_commit.message == 'ops: dispatch preburn for 1090 044ef5a'"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Verify immutable candidate ref
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin refs/heads/preburn/1090-044ef5a
          test "$(git rev-parse FETCH_HEAD)" = "044ef5a86df00346e785c36a1cfb552aa0aa195d"
      - name: Dispatch pre-burn verification on exact candidate ref
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/pre-release-verification.yml/dispatches" \
            -d '{"ref":"preburn/1090-044ef5a","inputs":{"release_tag":"v3.0.0-pr1090"}}'
