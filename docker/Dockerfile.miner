FROM rust:1.95 as builder
WORKDIR /app
COPY . .
RUN cargo build --release -p pulsedag-miner

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/pulsedag-miner /usr/local/bin/pulsedag-miner
CMD ["pulsedag-miner"]
