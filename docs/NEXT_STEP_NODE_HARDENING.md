# Next step node hardening

1. Implement `POST /mempool/sanitize`
2. Implement `GET /mempool/policy`
3. Expose sanitize counters in `/runtime`
4. Add journal events for sanitize runs
5. Persist snapshot after successful sanitize
6. Add mempool bound config via env vars
